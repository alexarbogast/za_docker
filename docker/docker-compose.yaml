version: "3"
services:
  ros:
    image: ros:noetic-tormach-dev
    build:
      context: .
      dockerfile: Dockerfile 
    container_name: noetic_tormach_dev
    network_mode: host
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    user: ros
    volumes:
      - ~/dev/ros_workspaces/noetic/:/home/ros/workspaces
